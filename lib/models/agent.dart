class Agent {
  String name;
  String photo;
  String fullPhoto;
  String role;
  static List<String> roles = [];

  Agent({
    required this.name,
    required this.photo,
    required this.fullPhoto,
    required this.role,
  });
}
